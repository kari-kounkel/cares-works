.vercel
.env*.local
node_modules
dist
.claude
# Office temp / lock files
~$*
*.tmp
.DS_Store

# Never commit env files (secrets)
.env

# Editor config
.vscode/

# Python bytecode
__pycache__/
*.pyc
vite.config.js.timestamp-*.mjs

# Private architecture write-ups — published as Artifacts, not committed.
# These name tables with row-level security disabled; this repo is public.
# The redacted record lives alongside them as .md (see docs/flowsuite-decision.md).
docs/flowsuite-decision.html
